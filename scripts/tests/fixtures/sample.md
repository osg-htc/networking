This is a small fixture with an unlabeled fenced block meant to exercise the `assign_fence_language.py` script.

``` bash
# Sample commands that should be detected by the heuristics
sudo apt-get update
sudo apt-get install -y curl
```

End.
